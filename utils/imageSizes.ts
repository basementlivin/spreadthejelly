// FULL-WIDTH IMAGES
// (e.g., Hero, Full-Width Image slices)
export const fullWidthSizes = `
  100vw, sm:100vw, md:100vw, lg:100vw, xl:100vw
`;

// PARTIAL-WIDTH IMAGES
// (e.g., Jelly Likes, Image Slider, etc.)
export const partialWidthSizes = `
  100vw, sm:100vw, md:50vw, lg:50vw, xl:33vw
`;
